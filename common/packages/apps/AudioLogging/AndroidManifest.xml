<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
                android:sharedUserId="android.uid.system"
          package="com.samsung.slsi.audiologging">
    <uses-sdk android:minSdkVersion="28" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_LOGS"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <protected-broadcast android:name="com.samsung.slsi.audio.action.LOGGING_RESULT" />

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true" >
            <activity
                android:name=".MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>
        <receiver
                android:name=".KeyStringReceiver"
                android:exported="true" >
            <intent-filter>
                <action android:name="android.provider.Telephony.SECRET_CODE" />
                <data
                    android:host="1121"
                    android:scheme="android_secret_code" />
            </intent-filter>
        </receiver>
        <receiver
                android:name=".LogActionReceiver"
                android:exported="true" >
            <intent-filter>
                <action android:name="com.samsung.slsi.audio.action.START_LOGGING" />
                <action android:name="com.samsung.slsi.audio.action.STOP_LOGGING" />
            </intent-filter>
        </receiver>
                <service android:name=".LogActionService" />
    </application>

</manifest>
